name: BuildDockerImage
on:
 push:
   branches: [master]
jobs:
   build:
     name: Greeting
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@v2
       - name: Run a multiple scripts
